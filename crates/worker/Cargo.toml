[package]
name = "taskforge-worker"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
redis = "0.25"
serde_json = "1"
taskforge-core = { path = "../task-core" }
